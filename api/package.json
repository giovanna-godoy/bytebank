{
  "name": "bytebank-api",
  "version": "1.0.0",
  "description": "Bytebank JSON Server API",
  "scripts": {
    "start": "json-server --watch db.json --port 3000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}